let userNames: string[] =['adim','ajiya','baneen','fatima','ammarah'];

if(userNames.length === 0){
    console.log("We need to find some users!");
}else{
    userNames = [];
    console.log("All user Namers have been removed. " + userNames.length);
}